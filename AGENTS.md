Rules:
1. When ready to commit, show what has changed/added and a commit message proposal, then wait for explicit approval before running `git commit`. Never assume standing permission to commit.
2. Never try to push
